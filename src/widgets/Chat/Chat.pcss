@import "@/shared/styles/mixin.pcss";

.chat {
  display: flex;
  height: 100%;

  &__body {
    display: flex;
    flex-direction: column;
    flex-grow: 1;
    padding-inline: 20px;
  }

  &__empty {
    @mixin flex-center;
    width: 100%;
  }
}
